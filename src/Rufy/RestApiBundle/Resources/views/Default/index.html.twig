<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Laravel PHP Framework</title>
        <style>

            body {
                margin:0;
                font-family:'Lato', sans-serif;
                text-align:center;
                color: #999;
            }

            .welcome {
                margin: 50px auto 0 auto;
            }

            a, a:visited {
                text-decoration:none;
            }

            h1 {
                font-size: 32px;
                margin: 16px 0 0 0;
            }
        </style>
    </head>
    <body>
        <div class="welcome">
            {% image '@RufyRestApiBundle/Resources/public/images/rufy.jpg' filter='jpegoptim' %}
                <img src="{{ asset_url }}" alt="Rufy" />
            {% endimage %}
        </div>
    </body>
</html>
